<?php
// connect to the database

$db = mysqli_connect('localhost', 'root', '', 'registration')

or die("Cannot connect to Server" . mysql_error()); //statement to establish a connection with the localhost(server).;

?>
